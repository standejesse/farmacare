import { StyleSheet } from 'react-native' 

let styles = {} 

export default styles = StyleSheet.create({
  imageContainer: {
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0    
  },
})